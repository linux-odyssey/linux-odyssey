<h1>Sessions</h1>
<div class='pagination'>
  <a href='/sessions?order=asc'>Ascending</a>
  <a href='/sessions?order=desc'>Descending</a>
</div>
<div>
  <table>
    <tr>
      <th>User</th>
      <th>Quest</th>
      <th>Status</th>
      <th>Created At</th>
      <th>Finished At</th>
      <th>Used Time</th>
      <th>Command Count</th>
      <th>Session</th>
    </tr>
    {{#each sessions}}
      <tr>
        <td>{{user}}</td>
        <td>{{quest}}</td>
        <td>{{status}}</td>
        <td>{{createdAt}}</td>
        <td>{{finishedAt}}</td>
        <td>{{usedTime}}</td>
        <td>{{commandCount}}</td>
        <td><a href='/sessions/{{_id}}'>Detail</a></td>
      </tr>
    {{/each}}
  </table>
</div>
<div class='pagination'>
  <a href='/sessions?nextKey={{nextKey}}&order={{order}}'>Next</a>
</div>